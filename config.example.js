/**
 * config.example.js — Configuration Template
 * =============================================================================
 * This is a TEMPLATE file. It is safe to commit to GitHub.
 * To set up the project locally:
 * 1. Copy this file and rename it to: config.js
 * 2. Replace all placeholder values with your real API keys
 * 3. The real config.js is ignored by Git (see .gitignore)
 * =============================================================================
 */

const CONFIG = {
  // Anthropic API key — https://console.anthropic.com/
  ANTHROPIC_KEY: 'sk-ant-your-key-here',

  // OpenAI API key — https://platform.openai.com/api-keys
  OPENAI_KEY: 'sk-your-key-here',

  // Supabase project URL — Supabase Dashboard → Project Settings → API
  SUPABASE_URL: 'https://your-project.supabase.co',

  // Supabase anonymous key — Supabase Dashboard → Project Settings → API
  SUPABASE_ANON: 'your-supabase-anon-key-here',
};
